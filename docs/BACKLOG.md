# Backlog

Status: **EMPTY — CORE COMPLETE**

There is no standing implementation, evaluation, CI, runtime, migration, or proof backlog for `skillz`.

The repository is in governed curation mode. Future work is discretionary and should be created only when someone actually chooses to do it.

Possible future activities include:

- review a newly discovered external skill or source;
- add a differentiated first-party skill;
- refresh an existing governed record after material upstream change;
- improve navigation if corpus growth makes it useful;
- revise bootstrap wording if real use exposes a genuine ambiguity.

These are opportunities, not unresolved completion requirements.

Do not preserve dormant issues merely as a reminder to keep working on the repository. Git history and the current curation policy are sufficient context when work resumes.
