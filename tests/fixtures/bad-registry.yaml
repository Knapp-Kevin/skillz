# Invalid-enum fixture for tests/skill-tools.test.mjs::audit-rejects-bad-registry-status
version: 1
updated: 2026-07-03

candidates:
  - id: test-entry
    name: Test Entry
    source: test
    category: [meta]
    status: yolo
    permission_tier: read-only
    rationale: fixture entry with an invalid status enum value.
